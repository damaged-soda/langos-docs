# adr 目录说明

用于存放跨仓或全局的 Architecture Decision Record（ADR）。推荐命名：`YYYYMMDD-short-name.md`。
目录职责与命名等规则以 `meta/conventions.md` 为准，本文件不重复说明。

每条 ADR 建议包含：背景与决策上下文、备选方案、决策、影响/后果、状态（提案/通过/废弃）。如果决策只影响某个仓库，可放在 `repos/<repo>/adr/`（如需）以便就近维护。***
