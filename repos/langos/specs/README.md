# specs/langos

用于存放 LangOS 内核自举时的需求规格文档，例如新增/修改协议、路由或治理规则的需求说明。

## 使用方式
- 每个需求一文，文件名建议 `<topic>-spec.md`，保持小写连字符。
- 推荐包含：背景/目标、触发意图、输入输出、步骤要点、确认点、安全约束、验收标准。
- 在改协议前先写需求；生成协议草稿后用 `simulate_protocol` 进行沙盒验证，确认通过再写入正式目录并更新索引。
