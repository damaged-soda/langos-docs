# 项目索引

职责：列出可治理/开发的目标仓库概览与导航（单文件 `repos/<name>.md`），不存放项目计划、specs/ADR 清单；这些属于 `projects/` 或 `specs/`/`adr/`。新增仓库按 `repos/<name>.md` 补充，并在此登记。

- **langos**：自然语言驱动的编程“操作系统”内核。索引/概览见 `repos/langos.md`，蓝图/治理文档在 doc_root 根目录；代码仓位于 `../langos/`。
